function [line, directionPoints) = complexLine(A,B)
    gamma = linspace(0,1,1e3*1);
    line = A*(1-gamma) + B*gamma;
    
    vicinity = [0.99 1.01];
    directionPoints = A*(1-vicinity) + B*vicinity;
    
    line = complex(real(line), imag(line));
    
